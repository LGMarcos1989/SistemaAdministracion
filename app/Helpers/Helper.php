<?php

     //Formatear fecha getFecha() - me devuelve una fecha en formato dd/mm/aaaa
    if(!function_exists()){
        function getFecha(){
          //  return Carbon::parse('');
        }
    }

      //Formatear monedas getMoney() - me pone la cifra en formato euros 
     if(!function_exists()){
        function getFecha(){
            return true;
        }
    }

    //Nombre del sistema appname() - me devuelve el nombre del sistema
     if(!function_exists()){
        function getFecha(){
            return true;
        }
    }

    //Calcular el total getTotal() - me devuelve el total de una cifra
     if(!function_exists()){
        function getFecha(){
            return true;
        }
    }


   

  

 


